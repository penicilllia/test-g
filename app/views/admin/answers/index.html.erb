<p id="notice"><%= notice %></p>

<h1><%= t('.header') %></h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <%= render @answers %>
</table>

<br>

<%= link_to t('.new'), new_admin_question_answer_path(@question) %><br>
<%= link_to t('.back'), edit_admin_question_path(@question) %>
